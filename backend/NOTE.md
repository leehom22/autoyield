npx supabase migration new create_orders_table

npx supabase db push


🚀 Next Level (Optional but HIGH VALUE)

If you want:


### Multi agent (last steps)
Forecast → Assistant Agent

Example:

if risk == "high":
    trigger assistant_agent